#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("enter value of a,b:");
    scanf("%d%d",&a,&b);
    if (a>b)
    {
        printf("a is big");
    }
    else
    {
        if (a>b)
        {
             printf("b is big");
        }
        else
        {
            printf("a=b");
        }
    }
    return 0;
}
