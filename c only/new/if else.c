#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("enter value of a,b:");
    scanf("%d%d",&a,&b);
    if (a>b)
    {
        prinf("a is big");
    }
    else
    {
        printf("b is big");
    }
    return 0;
}
