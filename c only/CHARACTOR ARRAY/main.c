#include <stdio.h>
#include <stdlib.h>

int main()
{
    char str[20];
    printf("ENTER STRING:");
    gets(str);          //work as scanf
    puts(str);          // works as printf
    return 0;
}
